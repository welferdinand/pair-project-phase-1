<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <h1 style="text-align: center;"> HOME </h1>
        <a href="/logout" type="button" class="btn btn-danger">Logout</a>
        <br>
        <br>
        <% category.forEach(ct => { %>
            <a href="/categories/<%= ct.id %>" type="button" class="btn btn-outline-primary"><%= ct.name %></a>  
        <% }) %>
    </div>
    <%- include('_links') %>
</body>
</html>
